# Session Context

## User Prompts

### Prompt 1

lets change the cli invoke so that i can flags. like ./webhook-tui --port=8000 --subdomain=nsolp44 --fwd=http://api.draydex.local --timeout=30 (minutes)

